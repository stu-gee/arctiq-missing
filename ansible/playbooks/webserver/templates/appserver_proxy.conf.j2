# Redirect traffic to appserver

server {
    listen 80 default_server;
    server_name webserver;

    location / {
        proxy_pass http://{{ appserver_ip }};
        proxy_set_header Authorization "";
        proxy_read_timeout 90s;
    }
}